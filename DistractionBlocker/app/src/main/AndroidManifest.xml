<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Boot receiver permission -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Device Admin (for deletion protection) -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />

    <!-- Kill settings process after guard fires so it can't re-surface -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <!-- Package visibility for the app picker.
         NOT QUERY_ALL_PACKAGES: Play treats the full installed-app inventory as sensitive
         and restricts that permission to a narrow set of app types (antivirus, file
         managers, browsers). This <queries> block is the policy-compliant alternative and
         is sufficient here, because the picker already enumerates via
         queryIntentActivities(MAIN/LAUNCHER) — it only ever needs apps the user can launch,
         which is exactly what this grants. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DistractionBlocker"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <!-- Launcher entry point. Controlled at runtime by the "Hide App Icon" toggle. -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.DistractionBlocker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity — started by the foreground service, notifications, and LauncherActivity. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DistractionBlocker"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <!-- Accessibility Service -->
        <service
            android:name=".service.BlockingAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Foreground service to keep the process alive.
             specialUse has no time limit (dataSync is capped at 6h/day on Android 14+). -->
        <service
            android:name=".service.FocusBlockerForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the focus-blocking accessibility service alive in the background" />
        </service>

        <!-- Boot receiver to auto-start on device reboot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- Alarm receiver: applies pending changes when their 24-hour delay elapses -->
        <receiver
            android:name=".receiver.PendingChangesReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.focusapp.blocker.PENDING_CHANGE_APPLY" />
            </intent-filter>
        </receiver>

        <!-- Developer escape hatch — trigger with: adb shell am broadcast -n com.focusapp.blocker/.receiver.DebugUnlockReceiver -->
        <receiver
            android:name=".receiver.DebugUnlockReceiver"
            android:exported="false" />

        <!-- Device Admin Receiver for deletion protection -->
        <receiver
            android:name=".receiver.FocusDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
